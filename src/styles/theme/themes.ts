import '@fontsource-variable/inter';

import { homeLight, homeLightColors } from './homeLight';
import { studioLight, studioLightColors } from './studioLight';

export const colors = {
  homeLight: homeLightColors,
  studioLight: studioLightColors,
};

export const themes = {
  homeLight,
  studioLight,
};
